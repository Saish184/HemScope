# Utilities

Add small, domain-independent helpers here only when needed, such as display formatting. Financial calculations, property filtering, and distance calculations belong in `domain/`. No speculative helpers are included.
