# Property logic

Future deterministic property filtering and comparisons belong here. Import shared contracts from `types/`; keep React, database access, and API calls outside this directory. No search implementation exists yet.
