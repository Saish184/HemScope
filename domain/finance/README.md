# Financial logic

The future deterministic financial engine belongs here, using `BuyerProfile`, `Property`, and `FinancialResult` from `types/`. Financial formulas, rounding, and validation must be explicit and tested when introduced. AI must never calculate financial results. No formulas or sample results exist yet.
